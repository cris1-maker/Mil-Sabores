# ============================
# ORDERS / CART (PUERTO 8083)
# ============================
server.port=8083

spring.datasource.url=jdbc:mysql://localhost:3306/milsabores?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Santiago
spring.datasource.username=ms_user
spring.datasource.password=Ms_pass123!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=15000

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false

# Config del carrito
cart.max.total=1000000

# URL interna del catálogo (para que orders llame a catálogo)
# Como todos corren en la misma EC2, localhost está bien aquí.
catalog.service.url=http://localhost:8082
